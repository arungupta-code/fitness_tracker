{
  "name": "fittrack",
  "private": true,
  "description": "Run API + React dev server together (fixes signup calling localhost:5000 with nothing listening).",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run start --prefix server\" \"npm start --prefix client\"",
    "start:server": "npm run start --prefix server",
    "start:client": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
